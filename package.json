{
  "name": "@tiro-health/smart-web-messaging",
  "version": "0.0.1",
  "description": "Smart Web Messaging SDK for Tiro Health",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:docs": "npm run build && cp -r dist docs/"
  },
  "files": [
    "/dist"
  ]
}
